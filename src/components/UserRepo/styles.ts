import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        gap: 8,

        padding: 24,
        marginBottom: 18,

        backgroundColor: "#22222F",
        borderRadius: 8
    },

    title: {
        fontSize: 16,
        fontWeight: "bold",

        color: "#FFF"
    },

    description: {
        fontSize: 12,
        overflow: "hidden",

        color: "#BFBFD4",
    }
})