import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        alignItems: "center",
        justifyContent: "center",
        gap: 12,

        marginTop: 124
    },

    emptyFavoriteRepoText: {
        fontSize: 16,
        fontWeight: "bold",

        textAlign: "center",
        color: "#FFF"
    },

    emptyFavoriteRepoDescription: {
        fontSize: 12,

        textAlign: "center",
        color: "#BFBFD4"
    }
})